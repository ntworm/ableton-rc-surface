# Docs Archive Pointer

Canonical docs in `docs/` must stay current and English.

Large historical docs were moved out of `docs/` to this local-only,
gitignored folder:

```text
archive/legacy-docs-2026-06-30/
```

Those files are retained for forensic context only. They are not current instructions.
They are not included in a clean clone or tester kit.

Rules:

- Do not copy old control names, version numbers, or architecture without checking current source.
- Do not treat old plans as executable.
- Do not use archived bug reports as proof of current behavior.
